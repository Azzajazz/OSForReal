- Run a process in user space
    - Page isolation between kernel and user space.
        X Implement commiting pages at different privilege levels
        - User space process address space isolation (give it it's own page directory).
    - Make a user space program.
        X Cross compile user code.
        - Put in the file system.
        - Load from kernel.
    - User space memory allocation.
    - Switch to user space.
    - Syscall interface.
    - Statically compile a driver into user space program.
    - Write a user space shell.

- PCI
    X Print vendor ids of existing devices.
    - Make a driver hookup mechanism for PCI devices.
        - I think we will need ELF parsing for this, one way or another.
    X Write a driver for something :)

- TagFS
    - Expand on docs as needed.
    X Write tfsfmt utility.
        X TESTS
            X Write deterministic random data to a file.
        X Format an image.
        X Create a file in a formatted image.
        X Create a tag in a formatted image.
        X Link a tag to a file.
    - FUSE driver.
    - Port TagFS to OSForReal.

--------------------------------------------------------------------------------
- BUGS
    - Writing to the hard drive (ata_write_sector) will sometimes hang when polling the status register.

--------------------------------------------------------------------------------
- Improvements
    - Tests
        - Some kernel functions may only be used for testing purposes. Maybe we can conditionally compile these so that using them outside of tests will throw a linker error.
        - (Hard) Tests can be discovered by searching the elf headers. This will give us the option of writing unit tests for modules in the module itself (although I don't think I want to do that).
    - warning: ./build/os-for-real.elf has a LOAD segment with RWX permissions
    - Use QEMU port 0xE9 for the debug console instead of serial output (no baud rate throttling).
    - zero-padding and space-padding for fmt_print.
    - Growable arrays? C doesn't have generics, so we will need to work out how to do this in as type-safe a way as possible.

--------------------------------------------------------------------------------
Toolchain:
- Make automatic toolchain installation script

--------------------------------------------------------------------------------
Kernel:
- PS/2 keyboard driver
- File system driver
- ELF parsing
- Executable loader
- Context switching

--------------------------------------------------------------------------------
Misc/low priority:
- Reduce build times of interrupt handlers.

--------------------------------------------------------------------------------
