- Tests + test runner
    - Bootloader entry point choices based on compilation flags.
    - Sort out includes between kernel and tests.
    - Write a test runner.
    - Write a test.

- Allocators
    - Linked list of memory in bootstrap.
    - Move gdt out of bootstrap.
    - Move stack out of bootstrap.
    - Write a page frame allocator.
    - Write a GPA or a bump allocator.

--------------------------------------------------------------------------------
Toolchain:
- Make automatic toolchain installation script

--------------------------------------------------------------------------------
Kernel:
- Move gdt out of the bootstrap code.
- Move stack out of the bootstrap code.

--------------------------------------------------------------------------------
Build scripts:
- Do something better than exported env var for debug mode
- Debug symbols in bootstrap code

--------------------------------------------------------------------------------
Misc:
- Reduce build times of interrupt handlers.

--------------------------------------------------------------------------------
