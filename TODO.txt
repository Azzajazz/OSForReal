- Allocators
    X Place page metadata.
    X Initialize page metadata.
        - Write tests for this.
    - Move gdt out of bootstrap.
    - Move stack out of bootstrap.
    - Write a page frame allocator.
    - Write a GPA or a bump allocator.

- TagFS
    - Expand on docs as needed.
    - Write tfsfmt utility.
        - TESTS
            X Write deterministic random data to a file.
        X Format an image.
        X Create a file in a formatted image.
        X Create a tag in a formatted image.
        X Link a tag to a file.
    - FUSE driver.
    - Port TagFS to OSForReal.

- Improvements
    - ASSERT does an early return in tests, which skips any cleanup. Think of a way to run cleanup no matter what (maybe look at other testing frameworks to see what they do)

- BUGS

--------------------------------------------------------------------------------
Toolchain:
- Make automatic toolchain installation script

--------------------------------------------------------------------------------
Kernel:
- Move gdt out of the bootstrap code.
- Move stack out of the bootstrap code.
- PS/2 keyboard driver
- File system driver
- ELF parsing
- Executable loader
- Context switching

--------------------------------------------------------------------------------
Build scripts:
- Test script

--------------------------------------------------------------------------------
Misc:
- Reduce build times of interrupt handlers.

--------------------------------------------------------------------------------
